server:
  port: 8401

spring:
  application:
    name: Better-Monitor-Admin
  boot:
    admin:
      ui:
        title: ${spring.application.name}
  security:
    user:
      name: better
      password: 123456