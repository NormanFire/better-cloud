spring:
  application:
    name: Better-Server-Test
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8001
      config:
        server-addr: ${nacos.url}:8001
        group: DEFAULT_GROUP
        prefix: better-server-test
        file-extension: yaml