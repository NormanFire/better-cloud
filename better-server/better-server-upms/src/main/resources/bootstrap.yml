spring:
  application:
    name: Better-Server-UPMS
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8001
      config:
        server-addr: ${nacos.url}:8001
        group: DEFAULT_GROUP
        prefix: better-server-upms
        file-extension: yaml